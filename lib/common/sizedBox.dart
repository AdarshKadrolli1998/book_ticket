import 'package:flutter/material.dart';

/// SizeBetween class used to leave space between two widgets
class SizeBetween extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 20,
    );
  }
}
